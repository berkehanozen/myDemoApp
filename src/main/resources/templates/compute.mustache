<title>My application deployed automatically</title>
  <h1>Hello, World!</h1>
    In the first form, enter a list of integers seperated by a newline character.
    Do the same thing for the second form as well.
    For the yhird form, enter a list of strings seperated by a newline chracter.
    For the fourth and fifth forms, enter an integer to each one.
    We flip the strings between the indexes, where indexes are the values of the numbers in the integer array which we find by using the two integers as indexes.
    
    Result is {{result}}


  <form action="compute" method="post">
        <textarea name="input1" rows="10" cols="30"></textarea>
        <textarea name="input2" rows="10" cols="30"></textarea>
        <textarea name="input3" rows="10" cols="30"></textarea>
        <textarea name="input4" rows="10" cols="30"></textarea>
        <textarea name="input5" rows="10" cols="30"></textarea>
        <br>
        <input type="submit">
  </form>
